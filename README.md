# data_security_course_project
README File for level 1, 2, 3

level 1 code saves the model in file named : centralized_best_model.pth
level 2 code saved the model in file named : federated_model.pth
level 3 code saved the model in file named : malicious_client_federated_model.pth

IF you want to run locally just make sure you have the following libraries imported / installed

import copy
import torch
import torchvision
import torch.nn as nn
import matplotlib.pyplot as plt
import torchvision.transforms as transforms
from torch.utils.data import random_split, DataLoader
import torch.nn.functional as F

When you download the code just run python file. Close any graph pop ups to let code continue executing and you will see the results on terminal.
